import { FC } from 'react';
import { PageWrapper } from '@/ui/components/PageWrapper';

export const ForgotPassword: FC = () => {
  return (
    <PageWrapper title="Forgot password" showSearch={false} showBack>
      in progress...
    </PageWrapper>
  );
};
